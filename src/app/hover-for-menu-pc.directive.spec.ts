import { HoverForMenuPcDirective } from './hover-for-menu-pc.directive';

describe('HoverForMenuPcDirective', () => {
  it('should create an instance', () => {
    // const directive = new HoverForMenuPcDirective();
    // expect(directive).toBeTruthy();
  });
});
